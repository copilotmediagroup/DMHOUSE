DMH SALES OS — INVITATION SEND CONFIRMATION PATCH v3.0.4

REPLACE ONLY:
src/pages/employee/DocumentStudio.tsx

DO NOT replace the full repository.
DO NOT replace package.json, package-lock.json, .env, Supabase files, or Edge Functions.

INSTALLATION
1. Open the ZIP.
2. In GitHub or Bolt, replace the existing file at:
   src/pages/employee/DocumentStudio.tsx
3. Save/commit the change.
4. Allow Vite to reload, or restart with npm run dev if needed.

WHAT THIS PATCH ADDS
- Send button changes to "Sending…" and disables during delivery.
- Duplicate clicks are blocked while sending and after success.
- Success toast appears immediately at the top-right.
- Failure toast clearly states that the invitation was not sent.
- After success, the send controls are replaced by an "Invitation Sent" screen.
- Confirmation screen shows buyer email, invite number, pending status, and expiration when available.
- Adds "View Transaction Alerts" and "Back to Portfolio" actions.
- Existing invitation history remains visible.

VALIDATION NOTE
The file was inspected and patched against the uploaded D MHOUSE-main project. Full npm build verification could not run in this environment because its internal npm registry was missing yallist@3.1.1. No other project files were changed.
