import {ArrowRight,CheckCircle2,FileSignature,Landmark,ShieldCheck} from 'lucide-react';
import {Link} from 'react-router-dom';
import {Card,Pill} from '../Primitives';
import type {BuyerWorkspaceDeal} from '../../store/BuyerPortalStore';
function nextAction(d:BuyerWorkspaceDeal){if(!d.nda_executed)return {label:'Sign NDA',icon:ShieldCheck,tone:'warning' as const};if(!d.purchase_agreement_executed&&d.offer_status==='accepted')return {label:'Sign purchase agreement',icon:FileSignature,tone:'warning' as const};if(d.status==='payment_pending')return {label:'Funding pending',icon:Landmark,tone:'blue' as const};return {label:'Deal in progress',icon:CheckCircle2,tone:'success' as const}}
export default function BuyerActionQueue({deals}:{deals:BuyerWorkspaceDeal[]}){const items=deals.slice(0,6);return <Card className="p-6"><div className="flex items-center justify-between"><div><p className="text-xs font-bold tracking-[.18em] text-blue-600">NEXT ACTIONS</p><h2 className="mt-2 text-xl font-semibold">Deal queue</h2></div><Pill tone="blue">{items.length} active</Pill></div><div className="mt-5 space-y-3">{items.length===0?<p className="rounded-2xl bg-slate-50 p-5 text-sm text-slate-500">No active transactions. Authorized portfolios remain available in Marketplace.</p>:items.map(d=>{const a=nextAction(d);const Icon=a.icon;return <Link key={d.room_id} to={`/buyer/portfolio/${d.portfolio_id}`} className="flex items-center gap-4 rounded-2xl border border-slate-200 p-4 transition hover:border-blue-300 hover:bg-blue-50/40"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-slate-100 text-slate-700"><Icon size={19}/></div><div className="min-w-0 flex-1"><p className="truncate font-semibold">{d.portfolio_name}</p><p className="mt-1 text-xs capitalize text-slate-500">{d.status.replace(/_/g,' ')}</p></div><Pill tone={a.tone}>{a.label}</Pill><ArrowRight size={17} className="text-slate-400"/></Link>})}</div></Card>}
