import {BriefcaseBusiness,Clock3,Download,ReceiptText} from 'lucide-react';
import {Card} from '../Primitives';
import type {BuyerWorkspaceSnapshot} from '../../store/BuyerPortalStore';
const money=(v:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v||0);
export default function BuyerWorkspaceMetrics({workspace}:{workspace:BuyerWorkspaceSnapshot}){const actionRequired=workspace.activeDeals.filter(d=>!d.nda_executed||!d.purchase_agreement_executed||d.status==='payment_pending').length;const metrics=[['Active deals',workspace.relationship.activeDeals,BriefcaseBusiness],['Action required',actionRequired,Clock3],['Lifetime purchases',workspace.relationship.totalPurchases,ReceiptText],['Lifetime volume',money(workspace.relationship.lifetimeSpend),Download]] as const;return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label,value,Icon])=><Card key={label} className="p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-400">{label}</p><p className="mt-3 text-2xl font-semibold">{value}</p></div><div className="grid h-11 w-11 place-items-center rounded-2xl bg-blue-50 text-blue-600"><Icon size={20}/></div></div></Card>)}</div>}
