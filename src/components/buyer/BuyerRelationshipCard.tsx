import {BadgeCheck,Clock3,Layers3,TrendingUp} from 'lucide-react';
import {Card,Pill} from '../Primitives';
import type {BuyerWorkspaceSnapshot} from '../../store/BuyerPortalStore';
export default function BuyerRelationshipCard({workspace}:{workspace:BuyerWorkspaceSnapshot}){const r=workspace.relationship;return <Card className="p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-bold tracking-[.18em] text-blue-600">RELATIONSHIP</p><h2 className="mt-2 text-xl font-semibold">Buyer profile</h2></div><Pill tone="success">{r.tier} tier</Pill></div><div className="mt-6 space-y-4">{[[BadgeCheck,'Company',workspace.buyer?.company_name||'Buyer'],[Layers3,'Preferred category',r.preferredCategory],[Clock3,'Average close time',`${r.averageCloseDays} days`],[TrendingUp,'Completed purchases',String(r.totalPurchases)]].map(([Icon,label,value]:any)=><div className="flex items-center gap-3" key={label}><div className="grid h-10 w-10 place-items-center rounded-2xl bg-slate-100 text-slate-600"><Icon size={18}/></div><div><p className="text-xs text-slate-400">{label}</p><p className="font-semibold">{value}</p></div></div>)}</div></Card>}
