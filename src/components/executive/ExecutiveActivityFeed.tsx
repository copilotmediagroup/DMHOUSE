import {ArrowRight} from 'lucide-react';
import {useNavigate} from 'react-router-dom';
import {Card} from '../Primitives';
import type {ExecutiveActivity} from './types';
const when=(value:string)=>new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}).format(new Date(value));
export default function ExecutiveActivityFeed({items}:{items:ExecutiveActivity[]}){const navigate=useNavigate();return <Card className="overflow-hidden"><div className="border-b border-slate-100 p-6"><p className="text-sm text-slate-500">Company motion</p><h3 className="mt-1 text-xl font-semibold">Daily activity feed</h3></div><div className="max-h-[520px] divide-y divide-slate-100 overflow-y-auto">{items.length?items.slice(0,14).map(item=><button key={item.id} onClick={()=>navigate(item.path)} className="flex w-full items-start gap-4 p-5 text-left hover:bg-slate-50"><span className="mt-1 h-2.5 w-2.5 shrink-0 rounded-full bg-blue-600"/><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><p className="font-semibold text-slate-900">{item.title}</p><span className="shrink-0 text-xs text-slate-400">{when(item.occurredAt)}</span></div><p className="mt-1 text-sm leading-5 text-slate-500">{item.detail}</p></div><ArrowRight size={17} className="mt-1 shrink-0 text-slate-300"/></button>):<div className="p-6 text-sm text-slate-500">No activity has been recorded yet.</div>}</div></Card>}
