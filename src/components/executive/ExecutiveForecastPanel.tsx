import {Card} from '../Primitives';
import type {ForecastStage} from './types';
const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function ExecutiveForecastPanel({stages,total,weighted}:{stages:ForecastStage[];total:number;weighted:number}){const max=Math.max(1,...stages.map(s=>s.value));return <Card className="p-6 md:p-7"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm text-slate-500">Pipeline forecast</p><h3 className="mt-1 text-xl font-semibold">Revenue probability by stage</h3></div><div className="text-left sm:text-right"><p className="text-sm text-slate-500">Weighted forecast</p><p className="text-2xl font-semibold">{money(weighted)}</p><p className="text-xs text-slate-400">{money(total)} open pipeline</p></div></div><div className="mt-7 space-y-5">{stages.map(stage=><div key={stage.label}><div className="flex items-center justify-between gap-4 text-sm"><div><span className="font-semibold">{stage.label}</span><span className="ml-2 text-slate-400">{stage.count} deals</span></div><div className="text-right"><span className="font-semibold">{money(stage.value)}</span><span className="ml-2 text-slate-400">{money(stage.weightedValue)} weighted</span></div></div><div className="mt-2 h-2.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{width:`${Math.max(stage.value?4:0,stage.value/max*100)}%`}}/></div></div>)}</div></Card>}
