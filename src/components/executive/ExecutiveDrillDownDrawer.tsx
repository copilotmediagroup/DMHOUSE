import {ArrowRight,ExternalLink,X} from 'lucide-react';
import {useEffect} from 'react';
import {useNavigate} from 'react-router-dom';
import {Pill} from '../Primitives';
import type {ExecutiveDrillDown} from './types';

const pillTone={default:'neutral',warning:'warning',danger:'danger',success:'success'} as const;
export default function ExecutiveDrillDownDrawer({drillDown,onClose}:{drillDown:ExecutiveDrillDown|null;onClose:()=>void}){const navigate=useNavigate();useEffect(()=>{if(!drillDown)return;const close=(event:KeyboardEvent)=>{if(event.key==='Escape')onClose()};window.addEventListener('keydown',close);return()=>window.removeEventListener('keydown',close)},[drillDown,onClose]);if(!drillDown)return null;const open=(path:string)=>{onClose();navigate(path)};return <div className="fixed inset-0 z-[80] flex justify-end bg-slate-950/35 backdrop-blur-[2px]" onMouseDown={onClose}><aside className="flex h-full w-full max-w-2xl flex-col bg-white shadow-2xl" onMouseDown={event=>event.stopPropagation()}><header className="border-b border-slate-200 p-6 md:p-8"><div className="flex items-start justify-between gap-5"><div><p className="text-sm font-semibold text-blue-600">{drillDown.eyebrow}</p><h2 className="mt-1 text-2xl font-semibold tracking-tight text-slate-950 md:text-3xl">{drillDown.title}</h2><p className="mt-2 max-w-xl text-sm leading-6 text-slate-500">{drillDown.summary}</p></div><button onClick={onClose} className="rounded-full border border-slate-200 p-2 text-slate-500 transition hover:bg-slate-100" aria-label="Close drill-down"><X size={20}/></button></div></header><div className="flex-1 overflow-y-auto"><div className="divide-y divide-slate-100">{drillDown.rows.length?drillDown.rows.map(row=><button key={row.id} onClick={()=>open(row.path)} className="flex w-full items-start justify-between gap-5 p-6 text-left transition hover:bg-slate-50 md:px-8"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-slate-950">{row.title}</p>{row.meta&&<Pill tone={pillTone[row.tone||'default']}>{row.meta}</Pill>}</div><p className="mt-1 text-sm leading-5 text-slate-500">{row.detail}</p>{row.value&&<p className="mt-3 text-lg font-semibold text-slate-900">{row.value}</p>}</div><ArrowRight size={19} className="mt-1 shrink-0 text-slate-300"/></button>):<div className="p-8 text-sm text-slate-500">{drillDown.emptyText}</div>}</div></div><footer className="border-t border-slate-200 p-5 md:px-8"><button onClick={()=>open(drillDown.rows[0]?.path||'/')} disabled={!drillDown.rows.length} className="flex w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white transition hover:bg-slate-800 disabled:cursor-not-allowed disabled:opacity-40"><ExternalLink size={17}/>Open first priority record</button></footer></aside></div>}
