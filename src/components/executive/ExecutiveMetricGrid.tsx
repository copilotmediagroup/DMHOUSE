import {ArrowUpRight} from 'lucide-react';
import {useNavigate} from 'react-router-dom';
import type {ExecutiveMetric} from './types';

const toneClass={default:'border-slate-200 bg-white',warning:'border-amber-200 bg-amber-50/60',danger:'border-red-200 bg-red-50/60',success:'border-emerald-200 bg-emerald-50/60'};
export default function ExecutiveMetricGrid({metrics}:{metrics:ExecutiveMetric[]}){const navigate=useNavigate();return <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4 2xl:grid-cols-6">{metrics.map(metric=><button key={metric.label} onClick={()=>navigate(metric.path)} className={`group rounded-[22px] border p-5 text-left shadow-[0_10px_35px_rgba(15,23,42,.045)] transition hover:-translate-y-0.5 hover:shadow-[0_16px_45px_rgba(15,23,42,.08)] ${toneClass[metric.tone||'default']}`}><div className="flex items-start justify-between gap-3"><p className="text-xs font-semibold uppercase tracking-[.12em] text-slate-500">{metric.label}</p><ArrowUpRight size={17} className="text-slate-300 transition group-hover:text-blue-600"/></div><p className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">{metric.value}</p><p className="mt-2 text-sm leading-5 text-slate-500">{metric.detail}</p></button>)}</section>}
