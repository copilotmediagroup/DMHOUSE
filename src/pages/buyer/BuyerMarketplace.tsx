import {useState} from 'react';
import {ArrowRight,BriefcaseBusiness,FileCheck2,LockKeyhole} from 'lucide-react';
import {Link} from 'react-router-dom';
import {Card,Pill,PrimaryButton} from '../../components/Primitives';
import {useBuyerPortalStore} from '../../store/BuyerPortalStore';
const money=(v:number|null)=>v==null?'Protected':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(v);
export default function BuyerMarketplace(){const {buyer,portfolios,loading,error}=useBuyerPortalStore();const [notice]=useState('');if(loading)return <div className="p-8">Loading secure marketplace…</div>;return <div className="p-5 lg:p-8"><div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-xs font-bold tracking-[.2em] text-blue-600">DATA MARKET HOUSE</p><h1 className="mt-2 text-3xl font-semibold">Secure Portfolio Marketplace</h1><p className="mt-2 text-sm text-slate-500">Private access for {buyer?.company_name||'approved buyers'}.</p></div><Pill tone={buyer?.status==='approved'?'success':'warning'}>{buyer?.status||'pending'}</Pill></div>{(error||notice)&&<p className="mt-5 rounded-2xl bg-red-50 p-4 text-sm text-red-700">{error||notice}</p>}{buyer?.status!=='approved'?<Card className="mt-8 p-8 text-center"><LockKeyhole className="mx-auto text-blue-600" size={34}/><h2 className="mt-4 text-xl font-semibold">Approval pending</h2><p className="mx-auto mt-2 max-w-lg text-sm text-slate-500">Your buyer profile has been received. Portfolio access appears here after Data Market House approves the account and authorizes specific files.</p></Card>:<div className="mt-7 grid gap-5 md:grid-cols-2">{portfolios.length===0?<Card className="p-8 md:col-span-2"><p className="text-sm text-slate-500">No portfolios are currently authorized for this account.</p></Card>:portfolios.map(p=><Card key={p.id} className="p-6"><div className="flex items-start justify-between gap-4"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600"><BriefcaseBusiness/></div><Pill tone={p.disclosureSigned?'success':'warning'}>{p.disclosureSigned?'Disclosure signed':'Review required'}</Pill></div><h2 className="mt-5 text-xl font-semibold">{p.name}</h2><p className="mt-1 text-sm text-slate-500">{p.category||'Debt portfolio'} · {p.accountCount.toLocaleString()} accounts</p><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Face value</p><p className="mt-1 font-semibold">{money(p.faceValue)}</p></div><div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-400">Asking price</p><p className="mt-1 font-semibold">{money(p.askingPrice)}</p></div></div><p className="mt-5 line-clamp-3 text-sm leading-6 text-slate-600">{p.description||'Private portfolio summary available in the secure deal room.'}</p><Link to={`/buyer/portfolio/${p.id}`}><PrimaryButton className="mt-5 w-full">Review portfolio <ArrowRight size={17} className="ml-2"/></PrimaryButton></Link>{p.canDownloadSample&&<p className="mt-3 flex items-center justify-center gap-2 text-xs text-emerald-700"><FileCheck2 size={14}/>Authorized sample access</p>}</Card>)}</div>}</div></div>}
