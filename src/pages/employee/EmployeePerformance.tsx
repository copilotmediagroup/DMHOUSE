import {Award,BarChart3,CircleDollarSign,Mail,Phone,RefreshCw,Target,Trophy} from 'lucide-react';
import {Card,SecondaryButton} from '../../components/Primitives';
import {usePerformanceStore} from '../../store/PerformanceStore';
const money=(n=0)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
export default function EmployeePerformance(){const {employeeDashboard:d,loading,error,refresh}=usePerformanceStore();return <div className="mx-auto max-w-[1400px] p-5 md:p-8 lg:p-10"><header className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-semibold text-blue-600">Your Performance · v2.4.2</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Your activity. Your pipeline. Your earnings.</h2><p className="mt-2 text-slate-500">A private scorecard built to show exactly what your work can produce.</p></div><SecondaryButton onClick={refresh}><RefreshCw className="mr-2" size={17}/>Refresh</SecondaryButton></header>{error&&<div className="mt-5 rounded-2xl bg-red-50 p-4 text-sm text-red-700">{error}</div>}{loading||!d?<div className="mt-7 text-sm text-slate-500">Loading your performance…</div>:<><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Hero icon={<CircleDollarSign/>} label="Potential commission in pipeline" value={money(d.potentialCommission)} note="Pending, estimated, or approved"/><Hero icon={<Award/>} label="Commission earned this month" value={money(d.commissionEarnedMonth)} note={`${money(d.commissionPaidLifetime)} paid lifetime`}/><Hero icon={<Trophy/>} label="Portfolios sold this month" value={d.filesSoldMonth} note={`${money(d.revenueMonth)} in sales`}/><Hero icon={<BarChart3/>} label="Current rank" value={d.rank?`#${d.rank}`:'Private'} note={d.rank?'Based on monthly production':'Owner has hidden rankings'}/></div><div className="mt-7 grid gap-6 lg:grid-cols-[1.3fr_.7fr]"><Card className="p-6"><p className="text-sm text-slate-500">Sales performance</p><h3 className="mt-1 text-xl font-semibold">Your production engine</h3><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-3"><Metric label="Pipeline value" value={money(d.pipelineValue)}/><Metric label="Open deals" value={d.openDeals}/><Metric label="Offers created" value={d.offersCreated}/><Metric label="Closing rate" value={`${d.closingRate}%`}/><Metric label="Average sale price" value={money(d.averageSalePrice)}/><Metric label="Total outreach" value={d.outreachCount}/></div></Card><Card className="p-6"><Target className="text-blue-600"/><p className="mt-5 text-sm text-slate-500">Next best move</p><h3 className="mt-1 text-xl font-semibold">{d.openDeals>0?'Advance an open deal':'Create new opportunity'}</h3><p className="mt-3 text-sm leading-6 text-slate-600">{d.openDeals>0?`You have ${d.openDeals} open deal${d.openDeals===1?'':'s'} worth ${money(d.pipelineValue)}. Focus on the buyer closest to an offer.`:'Your active pipeline is empty. Start new outreach and qualify a decision maker.'}</p><div className="mt-6 grid grid-cols-2 gap-3"><Metric label="Emails" value={d.emails}/><Metric label="Calls" value={d.calls}/></div></Card></div></>}</div>}
function Hero({icon,label,value,note}:{icon:React.ReactNode;label:string;value:string|number;note:string}){return <Card className="p-5"><div className="text-blue-600">{icon}</div><p className="mt-5 text-3xl font-semibold">{value}</p><p className="mt-1 text-sm font-medium text-slate-700">{label}</p><p className="mt-2 text-xs text-slate-500">{note}</p></Card>}
function Metric({label,value}:{label:string;value:string|number}){return <div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</p><p className="mt-2 text-xl font-semibold">{value}</p></div>}
