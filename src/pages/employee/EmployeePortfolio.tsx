import { Check, Download, Eye, LockKeyhole, Trophy } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Card, Pill, PrimaryButton, SecondaryButton } from '../../components/Primitives';
import { usePortfolioStore } from '../../store/PortfolioStore';
const money=(v:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(v);
export default function EmployeePortfolio(){const {active}=usePortfolioStore();if(!active)return <div className="p-10">No active portfolio.</div>;const payout=active.employeeCommissionType==='percentage'?active.askingPrice*(active.employeeCommissionValue/100):active.employeeCommissionValue;return <div className="mx-auto max-w-6xl p-5 md:p-8 lg:p-10"><header className="mb-8"><Pill tone="success">Active</Pill><h2 className="mt-3 text-3xl font-semibold">{active.name}</h2><p className="mt-2 text-slate-500">Approved employee sales workspace</p></header>{active.employeeCommissionVisible?<Card className="mb-6 overflow-hidden border-emerald-200 bg-emerald-50 p-7 md:p-9"><div className="flex flex-col gap-5 md:flex-row md:items-center"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-white text-emerald-700 shadow-sm"><Trophy size={28}/></div><div className="flex-1"><p className="text-sm font-bold uppercase tracking-wider text-emerald-700">Potential commission</p><p className="mt-2 text-4xl font-bold text-emerald-950">{money(payout)}</p><p className="mt-2 text-sm text-emerald-800">{active.employeeCommissionType==='percentage'?`${active.employeeCommissionValue}% of the approved ${money(active.askingPrice)} asking price`:`Flat commission on a ${money(active.askingPrice)} sale`}</p><p className="mt-3 text-xs font-medium text-emerald-700">Final commission is recorded when the deal closes.</p></div></div></Card>:<Card className="mb-6 flex items-center gap-4 border-blue-100 bg-blue-50 p-6"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-white text-blue-600"><Trophy size={23}/></div><div><p className="font-semibold text-blue-950">Commission available upon closing</p><p className="mt-1 text-sm text-blue-700">The owner has configured compensation for this portfolio. The amount is private until closing.</p></div></Card>}<div className="grid gap-6 lg:grid-cols-[1.35fr_.65fr]"><Card className="p-7 md:p-9"><p className="text-sm text-slate-500">Portfolio story</p><p className="mt-3 text-lg leading-8 text-slate-700">{active.description}</p><div className="mt-7 space-y-3">{active.sellingPoints.map(x=><div className="flex gap-3 rounded-2xl bg-slate-50 p-4" key={x}><Check className="text-emerald-600" size={19}/><span className="text-sm font-medium">{x}</span></div>)}</div></Card><div className="space-y-6"><Card className="p-6"><p className="text-sm text-slate-500">Asking price</p><p className="mt-2 text-3xl font-semibold">{money(active.askingPrice)}</p><p className="mt-4 text-sm text-slate-500">Use the approved asking price. All offers still require owner review.</p></Card><Card className="p-6"><p className="font-semibold">Masked sales file</p><p className="mt-2 text-sm text-slate-500">{active.maskedFile?.name||'No file available'}</p><Link to={`/employee/portfolio/${active.id}/preview`}><PrimaryButton className="mt-5 w-full" disabled={!active.maskedFile}><Eye className="mr-2" size={17}/>Preview complete masked file</PrimaryButton></Link><Link to="/employee/distribute"><SecondaryButton className="mt-3 w-full" disabled={!active.maskedFile}><Download className="mr-2" size={17}/>Send masked file to buyer</SecondaryButton></Link><p className="mt-3 text-xs leading-5 text-slate-400">Preview is view-only. Buyer delivery remains permission-controlled and logged.</p></Card><Card className="flex gap-3 p-5"><LockKeyhole className="shrink-0 text-blue-600"/><div><p className="text-sm font-semibold">Owner economics protected</p><p className="mt-1 text-xs leading-5 text-slate-500">Private minimum, acquisition cost and margin do not enter this workspace.</p></div></Card></div></div></div>}
