import { CheckCircle2, LayoutDashboard, MailCheck, Plus, Users } from 'lucide-react';
import { Link, useLocation, useNavigate } from 'react-router-dom';
import { Card, PrimaryButton, SecondaryButton } from '../../components/Primitives';

type State={buyerName?:string;buyerEmail?:string;portfolioName?:string;sentAt?:string;inviteNumber?:number};
export default function InvitationSent(){const location=useLocation();const nav=useNavigate();const state=(location.state||{}) as State;const sentAt=state.sentAt?new Date(state.sentAt):new Date();
 return <div className="relative min-h-[calc(100vh-2rem)] p-5 lg:p-8"><div className="fixed right-5 top-5 z-50 flex items-center gap-3 rounded-2xl bg-emerald-600 px-5 py-4 text-sm font-semibold text-white shadow-2xl"><CheckCircle2 size={20}/>Invitation sent successfully</div><div className="mx-auto flex min-h-[70vh] max-w-3xl items-center justify-center"><Card className="w-full p-7 text-center md:p-12"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-emerald-50 text-emerald-700"><MailCheck size={38}/></div><p className="mt-7 text-xs font-bold tracking-[.2em] text-emerald-700">INVITATION SENT</p><h1 className="mt-3 text-4xl font-semibold">Thank you. Your invite has been sent.</h1><p className="mx-auto mt-4 max-w-xl text-slate-500">The buyer can now open the secure portal and continue the transaction.</p><div className="mt-8 grid gap-3 rounded-3xl bg-slate-50 p-5 text-left sm:grid-cols-2"><Detail label="Buyer" value={state.buyerName||'Buyer'}/><Detail label="Email" value={state.buyerEmail||'Sent recipient'}/><Detail label="Portfolio" value={state.portfolioName||'Linked portfolio'}/><Detail label="Sent" value={sentAt.toLocaleString()}/><Detail label="Status" value="Sent"/><Detail label="Invitation" value={state.inviteNumber?`Invite ${state.inviteNumber} of 3`:'Active'}/></div><div className="mt-8 grid gap-3 sm:grid-cols-3"><Link to="/employee/deal-rooms"><PrimaryButton className="w-full"><Users className="mr-2" size={17}/>View Deal Room</PrimaryButton></Link><SecondaryButton className="w-full" onClick={()=>nav('/employee/documents',{replace:true})}><Plus className="mr-2" size={17}/>Send Another</SecondaryButton><Link to="/employee"><SecondaryButton className="w-full"><LayoutDashboard className="mr-2" size={17}/>Dashboard</SecondaryButton></Link></div></Card></div></div>}
function Detail({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-white p-4"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{label}</p><p className="mt-1 break-words font-semibold text-slate-800">{value}</p></div>}
