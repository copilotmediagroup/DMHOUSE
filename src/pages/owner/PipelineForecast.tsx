import {useEffect,useMemo,useState,type ReactNode} from 'react';
import {CalendarClock,CircleDollarSign,RefreshCw,Target,TrendingUp,Trophy} from 'lucide-react';
import {Card,PrimaryButton} from '../../components/Primitives';
import {supabase} from '../../lib/supabase';
import {usePipelineStore} from '../../store/PipelineStore';
import {useAgencyStore} from '../../store/AgencyStore';
const money=(n=0)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(n);
type Forecast={totalPipeline:number;weightedPipeline:number;expectedThisMonth:number;negotiations:number;portfoliosSent:number;winRate:number;averageDaysToClose:number};
const empty:Forecast={totalPipeline:0,weightedPipeline:0,expectedThisMonth:0,negotiations:0,portfoliosSent:0,winRate:0,averageDaysToClose:0};
export default function PipelineForecast(){const {opportunities,refresh}=usePipelineStore();const {agencies}=useAgencyStore();const [data,setData]=useState(empty);const [loading,setLoading]=useState(true);async function load(){setLoading(true);const {data,error}=await supabase.rpc('dmh_pipeline_forecast');if(!error&&data)setData(data as Forecast);await refresh();setLoading(false)}useEffect(()=>{load()},[]);const rows=useMemo(()=>opportunities.filter(o=>!['closed_won','closed_lost'].includes(o.stage)).sort((a,b)=>(b.askingPrice*b.probability)-(a.askingPrice*a.probability)),[opportunities]);return <div className="mx-auto max-w-[1500px] p-5 md:p-8 lg:p-10"><header className="flex flex-col gap-4 md:flex-row md:items-end md:justify-between"><div><p className="text-sm font-semibold text-blue-600">Revenue Forecast · v1.4.0</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Know what may close before it closes.</h2><p className="mt-2 text-slate-500">Live opportunity value, probability, timing and buyer ownership.</p></div><PrimaryButton onClick={load}><RefreshCw size={17} className={`mr-2 ${loading?'animate-spin':''}`}/>Refresh forecast</PrimaryButton></header><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={<CircleDollarSign/>} label="Total pipeline" value={money(data.totalPipeline)}/><Stat icon={<TrendingUp/>} label="Weighted pipeline" value={money(data.weightedPipeline)}/><Stat icon={<CalendarClock/>} label="Expected this month" value={money(data.expectedThisMonth)}/><Stat icon={<Target/>} label="Active negotiations" value={String(data.negotiations)}/><Stat icon={<Trophy/>} label="Win rate" value={`${data.winRate}%`}/><Stat icon={<CalendarClock/>} label="Average days to close" value={String(data.averageDaysToClose)}/><Stat icon={<Target/>} label="Portfolios sent" value={String(data.portfoliosSent)}/><Stat icon={<TrendingUp/>} label="Open opportunities" value={String(rows.length)}/></div><Card className="mt-7 overflow-hidden"><div className="border-b border-slate-100 p-6"><p className="text-sm text-slate-500">Revenue-ranked opportunity list</p><h3 className="mt-1 text-xl font-semibold">What requires management attention</h3></div>{rows.length===0?<p className="p-6 text-sm text-slate-500">Create an opportunity from the Pipeline board to begin forecasting.</p>:<div className="divide-y divide-slate-100">{rows.map(o=>{const a=agencies.find(x=>x.id===o.agencyId);return <div key={o.id} className="grid gap-4 p-5 md:grid-cols-[1.3fr_.8fr_.7fr_.7fr_.8fr]"><div><p className="font-semibold">{o.title}</p><p className="mt-1 text-sm text-slate-500">{a?.name||'Agency'} · {a?.ownerEmployeeName||'Unassigned'}</p></div><Mini label="Potential" value={money(o.askingPrice)}/><Mini label="Probability" value={`${o.probability}%`}/><Mini label="Weighted" value={money(o.askingPrice*o.probability/100)}/><Mini label="Expected close" value={o.expectedCloseDate?new Date(`${o.expectedCloseDate}T12:00:00`).toLocaleDateString():'Not scheduled'}/></div>})}</div>}</Card></div>}
function Stat({icon,label,value}:{icon:ReactNode;label:string;value:string}){return <Card className="p-5"><div className="text-blue-600">{icon}</div><p className="mt-5 text-3xl font-semibold">{value}</p><p className="mt-1 text-sm text-slate-500">{label}</p></Card>}
function Mini({label,value}:{label:string;value:string}){return <div><p className="text-xs text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div>}
