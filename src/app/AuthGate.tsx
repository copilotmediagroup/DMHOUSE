import { useEffect, useState, type FormEvent, type ReactNode } from 'react';
import { isSupabaseConfigured, supabase } from '../lib/supabase';
import { PrimaryButton, Card, Field, inputClass } from '../components/Primitives';
export default function AuthGate({children}:{children:ReactNode}){if(!isSupabaseConfigured)return <div className="grid min-h-screen place-items-center bg-[#08101f] p-6 text-white"><div className="max-w-xl rounded-3xl border border-white/10 bg-white/5 p-8"><p className="text-xs font-semibold tracking-[.24em] text-blue-400">DMH SALES OS · V3.1.7</p><h1 className="mt-3 text-2xl font-semibold">Supabase connection required</h1><p className="mt-3 text-sm leading-6 text-slate-300">Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to Bolt or Netlify, then redeploy.</p></div></div>;const [ready,setReady]=useState(false);const [session,setSession]=useState<any>(null);const [mode,setMode]=useState<'signin'|'signup'>('signin');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const [accountType,setAccountType]=useState<'owner'|'employee'|'buyer'>('owner');
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setReady(true)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);setReady(true)});return()=>data.subscription.unsubscribe()},[]);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const email=String(f.get('email'));const password=String(f.get('password'));const fullName=String(f.get('fullName')||(accountType==='owner'?'Owner':accountType==='buyer'?'Buyer':'Employee'));const companyName=String(f.get('companyName')||'');const phone=String(f.get('phone')||'');const joinCode=String(f.get('joinCode')||'').trim().toUpperCase();const result=mode==='signin'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{data:{full_name:fullName,account_type:accountType,join_code:joinCode,company_name:companyName,phone}}});if(result.error)setError(result.error.message);else if(mode==='signup'&&!result.data.session)setError('Account created. Confirm the email, then sign in.');setBusy(false)}
 if(!ready)return <div className="grid min-h-screen place-items-center bg-[#08101f] text-white">Loading Sales OS…</div>;if(session)return <>{children}</>;
 return <div className="grid min-h-screen place-items-center bg-[#08101f] p-5"><Card className="w-full max-w-md p-8"><p className="text-xs font-semibold tracking-[.24em] text-blue-600">DATA MARKET HOUSE</p><h1 className="mt-3 text-3xl font-semibold">Sales OS</h1><p className="mt-2 text-sm text-slate-500">Live Supabase workspace</p><div className="mt-7 grid grid-cols-2 rounded-xl bg-slate-100 p-1"><button className={`rounded-lg py-2 text-sm font-semibold ${mode==='signin'?'bg-white shadow-sm':''}`} onClick={()=>setMode('signin')}>Sign in</button><button className={`rounded-lg py-2 text-sm font-semibold ${mode==='signup'?'bg-white shadow-sm':''}`} onClick={()=>setMode('signup')}>Create account</button></div><form className="mt-6 space-y-4" onSubmit={submit}>{mode==='signup'&&<><div className="grid grid-cols-3 rounded-xl bg-slate-100 p-1"><button type="button" className={`rounded-lg py-2 text-xs font-semibold ${accountType==='owner'?'bg-white shadow-sm':''}`} onClick={()=>setAccountType('owner')}>Owner</button><button type="button" className={`rounded-lg py-2 text-xs font-semibold ${accountType==='employee'?'bg-white shadow-sm':''}`} onClick={()=>setAccountType('employee')}>Employee</button><button type="button" className={`rounded-lg py-2 text-xs font-semibold ${accountType==='buyer'?'bg-white shadow-sm':''}`} onClick={()=>setAccountType('buyer')}>Buyer</button></div><Field label="Full name"><input className={inputClass} name="fullName" required/></Field>{accountType==='employee'&&<Field label="Company join code" hint="Get this code from the Data Market House owner."><input className={inputClass} name="joinCode" required autoCapitalize="characters"/></Field>}{accountType==='buyer'&&<><Field label="Company name"><input className={inputClass} name="companyName" required/></Field><Field label="Phone"><input className={inputClass} name="phone"/></Field></>}</>}<Field label="Email"><input className={inputClass} name="email" type="email" required/></Field><Field label="Password"><input className={inputClass} name="password" type="password" minLength={6} required/></Field>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<PrimaryButton className="w-full" disabled={busy}>{busy?'Please wait…':mode==='signin'?'Sign in':accountType==='owner'?'Create owner account':accountType==='buyer'?'Create buyer account':'Join company'}</PrimaryButton></form></Card></div>}
