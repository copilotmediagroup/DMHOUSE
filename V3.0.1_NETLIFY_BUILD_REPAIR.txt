DMH Sales OS v3.0.1 — Netlify Build Repair

Corrections:
1. package.json now matches package-lock.json on Vite 5.4.21.
2. TypeScript target/lib moved from ES2020 to ES2021 so String.replaceAll is supported.
3. PortfolioStore dynamic Supabase select is cast at the query boundary to avoid the generated select-string parser treating a runtime field list as a malformed literal.
4. Version advanced to 3.0.1.

No database migration or Edge Function change is required.
Netlify environment variables remain:
- VITE_SUPABASE_URL
- VITE_SUPABASE_ANON_KEY
